#include <stdlib.h>
#include <unistd.h>

int main()
{
//    long long mb10 = 10*1024*1024*1024;

    for (int i = 0; i < 10; ++i)
    {
//        int* tmp = calloc(mb10, sizeof(int));
//        int* tmp1 = calloc(mb10, sizeof(int));
        sleep(1);
    }

    return 0;
}
